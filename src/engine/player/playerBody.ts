namespace SplitTime {
    /**
     * If set, level transitions will be made automatically when this body changes levels.
     * Additionally, some sprites may fade out when this body goes behind them.
     */
    export var playerBody: SplitTime.Body | null = null;
}
