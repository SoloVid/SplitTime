namespace G.button {
    export const GUI_CONFIRMATION = new splitTime.controls.Button(keyboard)
    export const GUI_CANCEL = new splitTime.controls.Button(keyboard)
    export const PAUSE = new splitTime.controls.Button(keyboard)
    export const PRIMARY_INTERACT = new splitTime.controls.Button(keyboard)
    export const SWITCH_CHARACTERS = new splitTime.controls.Button(keyboard)
    export const ATTACK = new splitTime.controls.Button(keyboard)
    export const SPECIAL = new splitTime.controls.Button(keyboard)
    export const JUMP = new splitTime.controls.Button(keyboard)
}
